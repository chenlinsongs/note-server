# 笔记软件技术方案

> 类飞书笔记应用 - 技术设计文档

项目名字后端取note-server,前端note-web,两个项目放在note-app下

---

## 一、功能概述

### 1.1 核心功能

| 功能模块 | 功能描述 |
|---------|---------|
| **所见即所得编辑** | 富文本编辑器，实时预览编辑效果，支持标题、列表、引用、代码块、表格等格式 |
| **Markdown支持** | 从外部复制Markdown文本后自动解析并渲染为富文本格式 |
| **全文检索** | 支持笔记标题、内容的中文全文检索，搜索结果高亮显示 |
| **图片处理** | 支持直接粘贴/拖拽图片到编辑器，自动上传并显示 |
| **文件夹管理** | 树形文件夹结构，支持无限层级嵌套 |
| **标签管理** | 为笔记添加多个标签，支持按标签筛选 |
| **版本控制** | 记录笔记编辑历史，支持版本回溯和对比 |

### 1.2 扩展功能（后续规划）

| 功能模块 | 功能描述 |
|---------|---------|
| **图表嵌入** | 在笔记中嵌入ECharts图表，支持可视化数据展示 |
| **画布/白板** | 嵌入自由画布，支持手绘、流程图等 |
| **思维导图** | 嵌入思维导图组件 |
| **代码编辑器** | 嵌入带语法高亮的代码编辑器 |

---

## 二、技术架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         前端 (Vue 3)                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │  Naive UI   │  │   TipTap    │  │    扩展组件 (ECharts等)   │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    后端 (Spring Boot)                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │  笔记服务    │  │  搜索服务    │  │       文件服务           │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
         │                  │                    │
         ▼                  ▼                    ▼
┌─────────────┐    ┌─────────────┐      ┌─────────────────┐
│   MySQL     │    │Elasticsearch│      │ 文件存储 (本地)  │
└─────────────┘    └─────────────┘      └─────────────────┘
```

### 2.2 前端技术栈

| 技术 | 版本 | 用途 |
|-----|------|-----|
| **Vue** | 3.x | 前端框架 |
| **Vite** | 5.x | 构建工具 |
| **Naive UI** | 2.x | UI组件库 |
| **TipTap** | 2.x | 富文本编辑器（基于ProseMirror） |
| **Pinia** | 2.x | 状态管理 |
| **Vue Router** | 4.x | 路由管理 |
| **Axios** | 1.x | HTTP客户端 |

### 2.3 后端技术栈

| 技术 | 版本 | 用途 |
|-----|------|-----|
| **Java** | 17+ | 开发语言 |
| **Spring Boot** | 3.2+ | 后端框架 |
| **Spring Data JPA** | - | ORM框架 |
| **MySQL** | 8.x | 主数据库 |
| **Elasticsearch** | 8.x | 全文检索引擎 |
| **IK Analyzer** | - | 中文分词插件 |
| **zjsonpatch** | - | JSON差异对比（版本控制） |
| **Thumbnailator** | - | 图片压缩处理 |

---

## 三、核心模块设计

### 3.1 编辑器设计

#### 3.1.1 TipTap编辑器

采用TipTap作为富文本编辑器，基于ProseMirror，具有以下优势：
- 模块化设计，按需加载扩展
- 原生支持Markdown解析
- 支持自定义Node和Mark
- 协同编辑友好

#### 3.1.2 内置扩展

```
基础扩展：
├── StarterKit（基础功能包）
├── Image（图片）
├── Table（表格）
├── TaskList（任务列表）
├── CodeBlockLowlight（代码高亮）
├── Link（链接）
├── Placeholder（占位符）
└── Markdown（Markdown粘贴支持）
```

#### 3.1.3 自定义扩展（嵌入块）

对于复杂组件（图表、画布等），采用自定义NodeView方式实现：

```
笔记内容JSON示例：
{
  "type": "doc",
  "content": [
    {
      "type": "paragraph",
      "content": [{"type": "text", "text": "这是一段普通文本"}]
    },
    {
      "type": "embedBlock",
      "attrs": {
        "blockUid": "blk_xxxxxxxxxxxx",
        "blockType": "chart"
      }
    }
  ]
}
```

**渲染机制**：
- TipTap遇到已注册的Node类型（如paragraph、heading）→ 自行渲染
- TipTap遇到`embedBlock`类型 → 调用自定义NodeView组件
- NodeView根据`blockUid`从后端获取数据，使用对应库（ECharts等）渲染

### 3.2 数据存储设计

#### 3.2.1 笔记内容存储

| 字段 | 类型 | 说明 |
|-----|------|-----|
| `content` | JSON | TipTap完整JSON格式，用于编辑器渲染 |
| `content_text` | TEXT | 纯文本内容，用于全文检索 |
| `content_markdown` | TEXT | Markdown格式（可选），用于导出 |

#### 3.2.2 嵌入块单独存储

复杂嵌入块（图表、画布等）数据量大，单独存储在`embed_blocks`表中：
- 笔记内容只存储`blockUid`引用
- 减少主表数据量
- 支持嵌入块独立版本控制
- 便于后续扩展

### 3.3 图片处理

#### 3.3.1 上传流程

```
用户粘贴/拖拽图片
       │
       ▼
  前端获取图片文件
       │
       ▼
  调用上传接口 POST /api/files/upload
       │
       ▼
  后端处理：
  ├── 生成唯一文件名
  ├── 压缩处理（可选）
  ├── 保存到本地/MinIO
  └── 记录到file_records表
       │
       ▼
  返回相对路径：/files/2024/12/abc123.png
       │
       ▼
  前端插入图片节点到编辑器
```

#### 3.3.2 URL存储策略

**存储相对路径**，动态拼接完整URL：
- 数据库存储：`/files/2024/12/abc123.png`
- 前端展示时拼接：`${BASE_URL}/files/2024/12/abc123.png`
- 域名变更只需修改`BASE_URL`配置

### 3.4 全文检索

#### 3.4.1 Elasticsearch索引设计

```json
{
  "settings": {
    "number_of_shards": 1,
    "number_of_replicas": 0,
    "analysis": {
      "analyzer": {
        "ik_smart_synonym": {
          "type": "custom",
          "tokenizer": "ik_smart"
        },
        "ik_max_synonym": {
          "type": "custom",
          "tokenizer": "ik_max_word"
        }
      }
    }
  },
  "mappings": {
    "properties": {
      "note_uid": { "type": "keyword" },
      "folder_uid": { "type": "keyword" },
      "title": {
        "type": "text",
        "analyzer": "ik_max_word",
        "search_analyzer": "ik_smart"
      },
      "content_text": {
        "type": "text",
        "analyzer": "ik_max_word",
        "search_analyzer": "ik_smart"
      },
      "tags": { "type": "keyword" },
      "word_count": { "type": "integer" },
      "created_at": { "type": "date","format": "yyyy-MM-dd HH:mm:ss||yyyy-MM-dd||epoch_millis" },
      "updated_at": { "type": "date","format": "yyyy-MM-dd HH:mm:ss||yyyy-MM-dd||epoch_millis" }
    }
  }
}
```

#### 3.4.2 检索流程

```
用户输入关键词
       │
       ▼
  后端调用ES搜索接口
       │
       ▼
  ES返回匹配结果（note_uid + 高亮片段）
       │
       ▼
  根据note_uid批量查询MySQL获取完整笔记信息
       │
       ▼
  合并数据返回前端（包含高亮显示的匹配片段）
```

### 3.5 版本控制

#### 3.5.1 增量存储方案

采用**JSON Patch**实现增量版本存储：

```
版本1（基准）：完整JSON快照
版本2：JSON Patch差异
版本3：JSON Patch差异
...
版本N：完整JSON快照（每N个版本保存一次完整快照）
版本N+1：JSON Patch差异
```

#### 3.5.2 版本恢复

```
恢复到版本M：
1. 找到最近的快照版本K（K ≤ M）
2. 从版本K开始，依次应用Patch直到版本M
3. 得到版本M的完整内容
```

#### 3.5.3 技术实现

使用`zjsonpatch`库：
- `JsonDiff.asJson(source, target)` - 计算差异
- `JsonPatch.apply(patch, source)` - 应用差异

### 3.6 文件夹结构

采用**邻接表 + 路径枚举**混合模型：

| 字段 | 说明 |
|-----|------|
| `uid` | 文件夹业务ID |
| `parent_uid` | 父文件夹UID（邻接表） |
| `path` | 完整路径，如 `/uid1/uid2/uid3`（路径枚举） |
| `level` | 层级深度 |

**优势**：
- 邻接表：便于单层查询和移动操作
- 路径枚举：便于查询所有子文件夹

---

## 四、数据库设计

### 4.1 设计原则

1. **双主键设计**
   - `id`：BIGINT自增，内部使用，高性能
   - `uid`：VARCHAR(64)，业务主键，对外暴露

2. **外键关联**
   - 外键字段关联业务`uid`（VARCHAR类型）
   - 不添加外键约束，由代码保证数据一致性

3. **软删除**
   - `deleted`：TINYINT(1)，0=正常，1=已删除
   - `deleted_at`：DATETIME，删除时间

4. **时间戳**
   - `created_at`：DEFAULT CURRENT_TIMESTAMP
   - `updated_at`：DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP

5. **字符集**
   - 使用 `utf8mb4_general_ci`

### 4.2 ER图

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   folders   │◄──────│    notes    │───────►│    tags     │
│             │       │             │        │             │
│ uid (PK)    │       │ uid (PK)    │        │ uid (PK)    │
│ parent_uid  │       │ folder_uid  │        │ name        │
│ name        │       │ title       │        └─────────────┘
│ path        │       │ content     │               │
└─────────────┘       │ content_text│               │
                      └─────────────┘               │
                             │                      │
              ┌──────────────┼──────────────┐      │
              │              │              │      │
              ▼              ▼              ▼      ▼
      ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
      │note_versions│ │embed_blocks │ │  note_tags  │
      │             │ │             │ │             │
      │ note_uid    │ │ note_uid    │ │ note_uid    │
      │ version     │ │ block_type  │ │ tag_uid     │
      │ patch_data  │ │ data        │ └─────────────┘
      └─────────────┘ └─────────────┘
                             │
                             ▼
                   ┌─────────────────────┐
                   │ embed_block_versions│
                   │                     │
                   │ block_uid           │
                   │ version             │
                   │ patch_data          │
                   └─────────────────────┘

      ┌─────────────┐
      │file_records │
      │             │
      │ uid (PK)    │
      │ note_uid    │
      │ file_path   │
      │ file_size   │
      └─────────────┘
```

### 4.3 表结构定义

#### 4.3.1 文件夹表 (folders)

```sql
CREATE TABLE folders (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '内部主键',
    uid VARCHAR(64) NOT NULL COMMENT '业务主键',
    parent_uid VARCHAR(64) DEFAULT NULL COMMENT '父文件夹UID',
    name VARCHAR(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NOT NULL COMMENT '文件夹名称',
    path VARCHAR(5000) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci DEFAULT '/' COMMENT '完整路径',
    level INT DEFAULT 0 COMMENT '层级深度',
    sort_order INT DEFAULT 0 COMMENT '排序序号',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted TINYINT(1) DEFAULT 0 COMMENT '软删除标记：0-正常，1-已删除',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    
    UNIQUE KEY uk_uid (uid),
    INDEX idx_parent_uid (parent_uid),
    INDEX idx_path (path(255)),
    INDEX idx_created_at (created_at),
    INDEX idx_updated_at (updated_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='文件夹表';
```

#### 4.3.2 笔记表 (notes)

```sql
CREATE TABLE notes (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '内部主键',
    uid VARCHAR(64) NOT NULL COMMENT '业务主键',
    folder_uid VARCHAR(64) DEFAULT NULL COMMENT '所属文件夹UID',
    title VARCHAR(500) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NOT NULL COMMENT '笔记标题',
    content JSON COMMENT '笔记内容（TipTap JSON格式）',
    content_text MEDIUMTEXT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci COMMENT '纯文本内容（用于搜索）',
    content_markdown MEDIUMTEXT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci COMMENT 'Markdown格式（用于导出）',
    word_count INT DEFAULT 0 COMMENT '字数统计',
    version INT DEFAULT 1 COMMENT '当前版本号',
    is_pinned TINYINT(1) DEFAULT 0 COMMENT '是否置顶',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted TINYINT(1) DEFAULT 0 COMMENT '软删除标记：0-正常，1-已删除',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    
    UNIQUE KEY uk_uid (uid),
    INDEX idx_folder_uid (folder_uid),
    INDEX idx_created_at (created_at),
    INDEX idx_updated_at (updated_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='笔记表';
```

#### 4.3.3 笔记版本表 (note_versions)

```sql
CREATE TABLE note_versions (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '内部主键',
    uid VARCHAR(64) NOT NULL COMMENT '业务主键',
    note_uid VARCHAR(64) NOT NULL COMMENT '笔记UID',
    version INT NOT NULL COMMENT '版本号',
    is_snapshot TINYINT(1) DEFAULT 0 COMMENT '是否为完整快照',
    patch_data JSON COMMENT '差异数据（JSON Patch格式）',
    snapshot_data JSON COMMENT '完整快照数据（当is_snapshot=1时）',
    content_hash VARCHAR(64) COMMENT '内容哈希（用于校验）',
    change_summary VARCHAR(500) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci COMMENT '变更摘要',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    
    UNIQUE KEY uk_uid (uid),
    UNIQUE KEY uk_note_version (note_uid, version),
    INDEX idx_note_uid (note_uid),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='笔记版本表';
```

#### 4.3.4 文件记录表 (file_records)

```sql
CREATE TABLE file_records (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '内部主键',
    uid VARCHAR(64) NOT NULL COMMENT '业务主键',
    note_uid VARCHAR(64) DEFAULT NULL COMMENT '关联笔记UID',
    original_name VARCHAR(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci COMMENT '原始文件名',
    stored_name VARCHAR(255) COMMENT '存储文件名',
    file_path VARCHAR(500) COMMENT '文件相对路径',
    file_size BIGINT COMMENT '文件大小（字节）',
    mime_type VARCHAR(100) COMMENT 'MIME类型',
    file_hash VARCHAR(64) COMMENT '文件哈希（用于去重）',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted TINYINT(1) DEFAULT 0 COMMENT '软删除标记：0-正常，1-已删除',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    
    UNIQUE KEY uk_uid (uid),
    INDEX idx_note_uid (note_uid),
    INDEX idx_file_hash (file_hash),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='文件记录表';
```

#### 4.3.5 标签表 (tags)

```sql
CREATE TABLE tags (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '内部主键',
    uid VARCHAR(64) NOT NULL COMMENT '业务主键',
    name VARCHAR(100) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NOT NULL COMMENT '标签名称',
    color VARCHAR(20) DEFAULT '#1890ff' COMMENT '标签颜色',
    usage_count INT DEFAULT 0 COMMENT '使用次数',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted TINYINT(1) DEFAULT 0 COMMENT '软删除标记：0-正常，1-已删除',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    
    UNIQUE KEY uk_uid (uid),
    UNIQUE KEY uk_name (name),
    INDEX idx_created_at (created_at),
    INDEX idx_updated_at (updated_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='标签表';
```

#### 4.3.6 笔记标签关联表 (note_tags)

```sql
CREATE TABLE note_tags (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '内部主键',
    note_uid VARCHAR(64) NOT NULL COMMENT '笔记UID',
    tag_uid VARCHAR(64) NOT NULL COMMENT '标签UID',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    
    UNIQUE KEY uk_note_tag (note_uid, tag_uid),
    INDEX idx_note_uid (note_uid),
    INDEX idx_tag_uid (tag_uid)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='笔记标签关联表';
```

#### 4.3.7 嵌入块表 (embed_blocks)

```sql
CREATE TABLE embed_blocks (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '内部主键',
    uid VARCHAR(64) NOT NULL COMMENT '业务主键（blockUid）',
    note_uid VARCHAR(64) NOT NULL COMMENT '所属笔记UID',
    block_type VARCHAR(50) NOT NULL COMMENT '块类型：chart/canvas/mindmap等',
    data TEXT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci COMMENT '块数据（JSON字符串）',
    version INT DEFAULT 1 COMMENT '当前版本号',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted TINYINT(1) DEFAULT 0 COMMENT '软删除标记：0-正常，1-已删除',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    
    UNIQUE KEY uk_uid (uid),
    INDEX idx_note_uid (note_uid),
    INDEX idx_block_type (block_type),
    INDEX idx_created_at (created_at),
    INDEX idx_updated_at (updated_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='嵌入块表';
```

#### 4.3.8 嵌入块版本表 (embed_block_versions)

```sql
CREATE TABLE embed_block_versions (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '内部主键',
    uid VARCHAR(64) NOT NULL COMMENT '业务主键',
    block_uid VARCHAR(64) NOT NULL COMMENT '嵌入块UID',
    version INT NOT NULL COMMENT '版本号',
    is_snapshot TINYINT(1) DEFAULT 0 COMMENT '是否为完整快照',
    patch_data TEXT COMMENT '差异数据',
    snapshot_data TEXT COMMENT '完整快照数据',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    
    UNIQUE KEY uk_uid (uid),
    UNIQUE KEY uk_block_version (block_uid, version),
    INDEX idx_block_uid (block_uid),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='嵌入块版本表';
```

### 4.4 业务ID生成规则

| 表 | 前缀 | 示例 |
|---|------|-----|
| folders | `fld_` | `fld_1735280000_abc123` |
| notes | `note_` | `note_1735280000_def456` |
| note_versions | `nver_` | `nver_1735280000_ghi789` |
| file_records | `file_` | `file_1735280000_jkl012` |
| tags | `tag_` | `tag_1735280000_mno345` |
| embed_blocks | `blk_` | `blk_1735280000_pqr678` |
| embed_block_versions | `bver_` | `bver_1735280000_stu901` |

生成规则：`前缀 + 时间戳 + 随机字符串`，总长度不超过64字符。

---

## 五、API设计

### 5.1 RESTful API规范

- 所有对外API使用业务`uid`作为资源标识
- 响应格式统一为：`{ code, message, data }`

### 5.2 核心接口列表

#### 文件夹管理

| 方法 | 路径 | 说明 |
|-----|------|-----|
| GET | `/api/folders` | 获取文件夹树 |
| POST | `/api/folders` | 创建文件夹 |
| PUT | `/api/folders/{uid}` | 更新文件夹 |
| DELETE | `/api/folders/{uid}` | 删除文件夹 |
| PUT | `/api/folders/{uid}/move` | 移动文件夹 |

#### 笔记管理

| 方法 | 路径 | 说明 |
|-----|------|-----|
| GET | `/api/notes` | 获取笔记列表 |
| GET | `/api/notes/{uid}` | 获取笔记详情 |
| POST | `/api/notes` | 创建笔记 |
| PUT | `/api/notes/{uid}` | 更新笔记 |
| DELETE | `/api/notes/{uid}` | 删除笔记 |
| GET | `/api/notes/{uid}/versions` | 获取版本列表 |
| GET | `/api/notes/{uid}/versions/{version}` | 获取指定版本 |
| POST | `/api/notes/{uid}/restore/{version}` | 恢复到指定版本 |

#### 搜索

| 方法 | 路径 | 说明 |
|-----|------|-----|
| GET | `/api/search?keyword=xxx` | 全文搜索 |

#### 文件上传

| 方法 | 路径 | 说明 |
|-----|------|-----|
| POST | `/api/files/upload` | 上传文件 |
| GET | `/files/{year}/{month}/{filename}` | 访问文件 |

#### 标签管理

| 方法 | 路径 | 说明 |
|-----|------|-----|
| GET | `/api/tags` | 获取标签列表 |
| POST | `/api/tags` | 创建标签 |
| DELETE | `/api/tags/{uid}` | 删除标签 |

#### 嵌入块

| 方法 | 路径 | 说明 |
|-----|------|-----|
| GET | `/api/blocks/{uid}` | 获取嵌入块数据 |
| PUT | `/api/blocks/{uid}` | 更新嵌入块数据 |

---


## 六、后续扩展规划

### 6.1 短期规划

- [ ] 笔记导出（Markdown、PDF、HTML）
- [ ] 笔记分享（生成分享链接）
- [ ] 快捷键支持
- [ ] 笔记模板

### 6.2 中期规划

- [ ] 用户登录与权限管理
- [ ] 多人协同编辑
- [ ] 笔记评论
- [ ] 回收站

### 6.3 长期规划

- [ ] AI智能助手
- [ ] 知识图谱
- [ ] 移动端适配
- [ ] 离线支持（PWA）

---

## 七、项目结构

### 7.1 前端项目结构

```
notes-web/
├── public/
│   └── favicon.ico
│
├── src/
│   ├── api/                              # API 请求层
│   │   ├── request.ts                    # Axios 封装
│   │   ├── notes.ts                      # 笔记 API
│   │   ├── files.ts                      # 文件 API
│   │   ├── search.ts                     # 搜索 API
│   │   ├── versions.ts                   # 版本 API
│   │   └── tags.ts                       # 标签 API
│   │
│   ├── assets/                           # 静态资源
│   │   ├── icons/
│   │   └── images/
│   │
│   ├── components/                       # 组件
│   │   ├── common/                       # 通用组件
│   │   │   ├── AppLogo.vue
│   │   │   └── EmptyState.vue
│   │   │
│   │   ├── layout/                       # 布局组件
│   │   │   ├── AppLayout.vue             # 主布局
│   │   │   ├── Sidebar.vue               # 侧边栏
│   │   │   └── Header.vue                # 顶部栏
│   │   │
│   │   ├── editor/                       # 编辑器组件
│   │   │   ├── TiptapEditor.vue          # 编辑器主组件
│   │   │   ├── MenuBar.vue               # 工具栏
│   │   │   ├── BubbleMenu.vue            # 浮动菜单
│   │   │   ├── SlashCommand.vue          # 斜杠命令
│   │   │   └── extensions/               # TipTap 扩展
│   │   │       ├── index.ts              # 扩展注册
│   │   │       ├── ImagePaste.ts         # 图片粘贴
│   │   │       ├── SlashCommands.ts      # 斜杠命令
│   │   │       └── placeholder/          # 预留扩展目录
│   │   │           ├── ChartBlock.ts
│   │   │           └── CanvasBlock.ts
│   │   │
│   │   ├── note/                         # 笔记相关组件
│   │   │   ├── NoteList.vue              # 笔记列表
│   │   │   ├── NoteCard.vue              # 笔记卡片
│   │   │   └── NoteActions.vue           # 操作菜单
│   │   │
│   │   ├── search/                       # 搜索组件
│   │   │   ├── SearchBar.vue             # 搜索框
│   │   │   └── SearchResults.vue         # 搜索结果
│   │   │
│   │   ├── version/                      # 版本组件
│   │   │   ├── VersionPanel.vue          # 版本面板
│   │   │   ├── VersionList.vue           # 版本列表
│   │   │   └── VersionDiff.vue           # 版本对比
│   │   │
│   │   └── tag/                          # 标签组件
│   │       ├── TagSelect.vue
│   │       └── TagManager.vue
│   │
│   ├── composables/                      # 组合式函数
│   │   ├── useEditor.ts                  # 编辑器逻辑
│   │   ├── useAutoSave.ts                # 自动保存
│   │   ├── useImageUpload.ts             # 图片上传
│   │   ├── useSearch.ts                  # 搜索逻辑
│   │   └── useVersion.ts                 # 版本逻辑
│   │
│   ├── config/                           # 配置
│   │   └── index.ts                      # 应用配置
│   │
│   ├── router/                           # 路由
│   │   └── index.ts
│   │
│   ├── stores/                           # Pinia 状态
│   │   ├── noteStore.ts                  # 笔记状态
│   │   ├── tagStore.ts                   # 标签状态
│   │   └── appStore.ts                   # 应用状态
│   │
│   ├── styles/                           # 样式
│   │   ├── variables.scss                # 变量
│   │   ├── editor.scss                   # 编辑器样式
│   │   └── main.scss                     # 主样式
│   │
│   ├── types/                            # TypeScript 类型
│   │   ├── api.d.ts                      # API 类型
│   │   ├── note.d.ts                     # 笔记类型
│   │   └── editor.d.ts                   # 编辑器类型
│   │
│   ├── utils/                            # 工具函数
│   │   ├── content.ts                    # 内容处理
│   │   ├── file.ts                       # 文件处理
│   │   ├── date.ts                       # 日期处理
│   │   └── markdown.ts                   # Markdown 工具
│   │
│   ├── views/                            # 页面
│   │   ├── HomeView.vue                  # 首页（笔记列表）
│   │   ├── EditorView.vue                # 编辑页
│   │   ├── SearchView.vue                # 搜索页
│   │   └── TrashView.vue                 # 回收站
│   │
│   ├── App.vue                           # 根组件
│   └── main.ts                           # 入口
│
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── .env                                  # 环境变量
├── .env.development
├── .env.production
└── README.md
```

### 7.2 后端项目结构

```
notes-server/
├── src/main/java/com/notes/
│   ├── controller/             # 控制器层
│   ├── service/                # 服务层
│   │   └── impl/
│   ├── repository/             # 数据访问层
│   ├── entity/                 # JPA 实体
│   ├── document/               # ES 文档
│   ├── dto/                    # 数据传输对象
│   │   ├── request/
│   │   └── response/
│   ├── config/                 # 配置类
│   ├── util/                   # 工具类
│   ├── converter/              # 类型转换器
│   ├── exception/              # 异常处理
│   └── NotesApplication.java
├── src/main/resources/
│   ├── application.yml
│   ├── application-dev.yml
│   └── application-prod.yml
└── pom.xml
```

---

## 八、部署方案

### 8.1 组件清单

| 组件 | 端口 | 说明 |
|------|------|------|
| Frontend (Nginx) | 80 | 前端静态资源 |
| Backend (Spring Boot) | 8080 | 后端 API |
| MySQL | 3306 | 主数据库 |
| Elasticsearch | 9200 | 全文检索 |
| 文件存储 | - | 本地目录或 MinIO |

### 8.2 Docker Compose 部署

```yaml
services:
  frontend:
    build: ./notes-frontend
    ports: ["80:80"]
    
  backend:
    build: ./notes-backend
    ports: ["8080:8080"]
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/notes_db
      - SPRING_ELASTICSEARCH_URIS=http://elasticsearch:9200
    volumes:
      - uploads:/app/uploads
      
  mysql:
    image: mysql:8.0
    environment:
      - MYSQL_DATABASE=notes_db
    volumes:
      - mysql_data:/var/lib/mysql
      
  elasticsearch:
    image: elasticsearch:8.11.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
    volumes:
      - es_data:/usr/share/elasticsearch/data
```

---

## 九、开发计划

| 阶段 | 任务 | 预计时间 |
|------|------|----------|
| Phase 1 | 项目初始化（前后端骨架 + 数据库） | 1 天 |
| Phase 2 | 核心编辑器（TipTap + Markdown + 工具栏） | 3 天 |
| Phase 3 | 笔记 CRUD（API + 列表 + 自动保存） | 2 天 |
| Phase 4 | 图片功能（上传 + 粘贴 + 相对路径） | 2 天 |
| Phase 5 | 版本控制（增量存储 + 历史 + 恢复） | 2 天 |
| Phase 6 | 全文搜索（ES + IK 分词 + 高亮） | 2 天 |
| Phase 7 | 优化完善（UI + 性能 + 测试） | 2 天 |
| **总计** | | **约 14 天** |

---

## 十、后续扩展规划

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 图表扩展 | 集成 ECharts，支持多种图表类型 | P1 |
| 白板扩展 | 集成 Excalidraw，支持手绘 | P1 |
| 流程图扩展 | 集成 Mermaid，支持流程图/思维导图 | P2 |
| 用户系统 | 登录注册、权限管理 | P2 |
| 协作编辑 | 多人实时协作 | P3 |
| 移动端适配 | 响应式设计或独立 App | P3 |
| 导入导出 | 支持 Word、PDF 等格式 | P3 |

---

## 十一、环境要求

| 组件 | 最低要求 |
|-----|---------|
| CPU | 2核 |
| 内存 | 4GB |
| 磁盘 | 20GB |
| Docker | 20.x+ |
| Docker Compose | 2.x+ |


### 软件版本汇总

| 软件 | 版本要求 | 用途 |
|-----|---------|-----|
| JDK | 17+ | 后端运行环境 |
| Maven | 3.8+ | 后端构建工具 |
| Node.js | 18+ | 前端运行环境 |
| npm/pnpm | 最新版 | 前端包管理 |
| MySQL | 8.x | 主数据库 |
| Elasticsearch | 8.x | 全文检索 |
| Docker | 20+ | 可选，容器化部署 |

---

## 十二、总结

本方案设计了一个类飞书的笔记软件，采用Vue 3 + Spring Boot技术栈，核心特性包括：

1. **所见即所得编辑**：基于TipTap实现，支持丰富的文本格式
2. **Markdown支持**：粘贴即转换，无缝衔接
3. **全文检索**：Elasticsearch + IK分词，高效中文搜索
4. **图片处理**：粘贴即上传，相对路径存储
5. **版本控制**：JSON Patch增量存储，节省空间
6. **可扩展架构**：自定义嵌入块，支持图表、画布等扩展

数据库采用双主键设计（内部ID + 业务UID），外键不添加约束，代码层面保证一致性，提供更高的灵活性。


## 十三、验证启动
### 1. 验证 ES 是否启动成功

打开**新的终端窗口**，执行：

```bash
curl http://localhost:9200
```

成功返回：
```json
{
  "name" : "LINSONGCHEN2025",
  "cluster_name" : "elasticsearch",
  "cluster_uuid" : "xxxxx",
  "version" : {
    "number" : "8.11.0",
    ...
  },
  "tagline" : "You Know, for Search"
}
```

### 2. 验证 IK 分词插件

```bash
curl http://localhost:9200/_cat/plugins
```

应该看到 `analysis-ik` 插件。

### 3. 测试中文分词

```bash
curl -X POST "http://localhost:9200/_analyze?pretty" -H 'Content-Type: application/json' -d'
{
  "analyzer": "ik_smart",
  "text": "笔记软件技术方案"
}'
```

---

### 启动整个笔记应用

确认 ES 运行正常后，按顺序启动：

#### 终端1：Elasticsearch（已运行）
保持当前终端运行

#### 终端2：启动后端
```bash
cd /Users/linsong.chen/VisualStudio/note-app/note-server
mvn spring-boot:run
```

#### 终端3：启动前端
```bash
cd /Users/linsong.chen/VisualStudio/note-app/note-web
npm install
npm run dev
```

#### 访问应用
打开浏览器：**http://localhost:3000**

---

### 服务端口汇总

| 服务 | 端口 | 地址 |
|-----|------|-----|
| Elasticsearch | 9200 | http://localhost:9200 |
| 后端 API | 8080 | http://localhost:8080 |
| 前端页面 | 3000 | http://localhost:3000 |

